# Node / pnpm
node_modules/
.pnpm-store/
*.log
npm-debug.log*
pnpm-debug.log*

# Build output
dist/
build/
.next/
out/
.turbo/

# Anchor / Solana
target/
.anchor/
test-ledger/
*.so

# Rust
Cargo.lock
**/*.rs.bk

# Env + secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/
keypairs/
!keypairs/.gitkeep

# IDE + Claude Code
.vscode/
.idea/
.claude/settings.local.json
*.swp
.DS_Store
Thumbs.db

# Test + coverage
coverage/
.nyc_output/
playwright-report/
test-results/

# Misc
*.tsbuildinfo
.cache/

# Working docs — only README.md ships to the public GitHub remote.
# All other root-level *.md files and the specs/ folder stay local-only
# (strategy docs, build plans, deployment runbooks, internal specs).
/*.md
!/README.md
specs/

.vercel

# Local-only video pipeline (Remotion + Playwright captures + ElevenLabs audio).
# Final exported MP4 lives elsewhere (YouTube unlisted / Vercel asset) and is
# linked from the README — the source project doesn't need to ship.
/video/

# Local-only deck preview artifacts
/deck-preview-*.png
/app/snap-deck.mjs

